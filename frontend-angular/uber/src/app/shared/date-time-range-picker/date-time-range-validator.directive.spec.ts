import { DateTimeRangeValidatorDirective } from './date-time-range-validator.directive';

describe('DateTimeRangeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DateTimeRangeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
