/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { UnitPipe } from './unit.pipe';

describe('UnitPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitPipe(null);
    expect(pipe).toBeTruthy();
  });
});
